# Supabase — включает авторизацию и облачный синк прогресса.
# Без этих переменных курс работает в гость-режиме (прогресс в localStorage).
# Значения: Supabase Dashboard → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
